/**
 * @nova-app/engine — pure arbitrage detection logic.
 * Stub for Phase 0 — see docs/10_ARBITRAGE_ENGINE.md for the full spec.
 */

export const NOVA_ENGINE_VERSION = '0.1.0';

export function findOpportunities(): readonly never[] {
  return [];
}
